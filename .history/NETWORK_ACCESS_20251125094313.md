# 🌐 Hướng Dẫn Cho Phép Người Khác Truy Cập Localhost

Có 3 cách để cho phép người khác truy cập ứng dụng localhost của bạn:

## 📋 Cách 1: Truy Cập Qua Mạng Nội Bộ (Đơn Giản Nhất)

### Bước 1: Tìm IP Address của máy bạn

**Trên Windows:**

```powershell
ipconfig
```

Tìm dòng `IPv4 Address` (thường là `192.168.x.x` hoặc `10.x.x.x`)

**Trên Mac/Linux:**

```bash
ifconfig
# hoặc
ip addr
```

### Bước 2: Di chuyển vào thư mục dự án

**⚠️ QUAN TRỌNG:** Bạn phải ở trong thư mục dự án trước khi chạy lệnh!

**Trên Windows (PowerShell):**

```powershell
cd "C:\Users\LNV\Documents\5Code\FE-Blue-Code"
```

**Trên Windows (CMD):**

```cmd
cd C:\Users\LNV\Documents\5Code\FE-Blue-Code
```

### Bước 3: Chạy server với quyền truy cập mạng

**✅ Đã tạo file `.env` với cấu hình HOST=0.0.0.0**

Bây giờ bạn chỉ cần chạy lệnh bình thường:

```bash
npm start
```

Hoặc:

```bash
npm run dev:network
```

**Lưu ý:** File `.env` đã được tạo tự động. Nếu bạn muốn tắt truy cập mạng, chỉ cần xóa hoặc comment dòng `HOST=0.0.0.0` trong file `.env`.

**Các cách khác (nếu không dùng file .env):**

**Trên Windows (PowerShell):**
```powershell
$env:HOST="0.0.0.0"; npm start
```

**Trên Windows (CMD):**
```cmd
set HOST=0.0.0.0 && npm start
```

**Trên Mac/Linux:**
```bash
HOST=0.0.0.0 npm start
```

### Bước 4: Chia sẻ địa chỉ với người khác

Sau khi server chạy, bạn sẽ thấy thông báo như:

```
Compiled successfully!

You can now view react-typescript-tailwind in the browser.

  Local:            http://localhost:3000
  On Your Network:  http://192.168.1.100:3000
```

Chia sẻ địa chỉ **"On Your Network"** (ví dụ: `http://192.168.1.100:3000`) với người khác.

**Lưu ý:**

- Cả hai máy phải cùng mạng WiFi/LAN
- Có thể cần tắt Windows Firewall hoặc cho phép port 3000
- Đảm bảo backend API cũng được cấu hình tương tự

---

## 📋 Cách 2: Sử Dụng File .env (Khuyến Nghị - ✅ Đã được tạo sẵn)

### ✅ File `.env` đã được tạo tự động!

File `.env` đã có trong thư mục dự án với nội dung:
```env
HOST=0.0.0.0
```

### Chạy server bình thường

```bash
npm start
```

Server sẽ tự động bind vào `0.0.0.0` và cho phép truy cập từ mạng.

**Nếu bạn muốn tắt truy cập mạng:**
- Xóa file `.env`, hoặc
- Comment dòng `HOST=0.0.0.0` trong file `.env`

---

## 📋 Cách 3: Sử Dụng Ngrok (Cho Truy Cập Từ Internet)

Nếu bạn muốn người khác truy cập từ internet (không cần cùng mạng):

### Bước 1: Cài đặt Ngrok

Tải từ: https://ngrok.com/download

### Bước 2: Đăng ký và lấy auth token

1. Đăng ký tài khoản miễn phí tại https://ngrok.com
2. Lấy auth token từ dashboard

### Bước 3: Cấu hình Ngrok

```bash
ngrok config add-authtoken YOUR_AUTH_TOKEN
```

### Bước 4: Chạy ứng dụng và Ngrok

**Terminal 1:** Chạy ứng dụng

```bash
npm start
```

**Terminal 2:** Chạy Ngrok

```bash
ngrok http 3000
```

Ngrok sẽ cung cấp URL công khai như: `https://abc123.ngrok.io`

Chia sẻ URL này với người khác để họ truy cập.

---

## ⚠️ Lưu Ý Quan Trọng

### 1. Cấu hình Backend API

Nếu backend API của bạn đang chạy trên `localhost:5000`, bạn cần:

**Cập nhật file `.env`:**

```env
HOST=0.0.0.0
PORT=3000
REACT_APP_API_URL=http://YOUR_IP_ADDRESS:5000
REACT_APP_SOCKET_URL=http://YOUR_IP_ADDRESS:5000
```

Thay `YOUR_IP_ADDRESS` bằng IP thực của máy bạn.

### 2. Windows Firewall

Có thể cần cho phép port 3000 và 5000 trong Windows Firewall:

1. Mở **Windows Defender Firewall**
2. Chọn **Advanced settings**
3. Chọn **Inbound Rules** → **New Rule**
4. Chọn **Port** → **Next**
5. Chọn **TCP** và nhập port `3000` và `5000`
6. Chọn **Allow the connection**
7. Áp dụng cho tất cả profiles

### 3. Router/Network Settings

- Đảm bảo cả hai máy cùng mạng
- Một số router có thể chặn giao tiếp giữa các thiết bị (AP Isolation)

---

## 🚀 Script Nhanh

Đã thêm script `dev:network` vào `package.json`:

**⚠️ LƯU Ý:** Phải cd vào thư mục dự án trước!

```powershell
# Bước 1: Di chuyển vào thư mục dự án
cd "C:\Users\LNV\Documents\5Code\FE-Blue-Code"

# Bước 2: Chạy script
npm run dev:network
```

Script này sẽ tự động bind vào `0.0.0.0` và cho phép truy cập từ mạng.

**Hoặc sử dụng PowerShell trực tiếp:**

```powershell
cd "C:\Users\LNV\Documents\5Code\FE-Blue-Code"
$env:HOST="0.0.0.0"; npm start
```

---

## 📝 Tóm Tắt

**Cho mạng nội bộ:**

1. **Di chuyển vào thư mục dự án:**
   ```powershell
   cd "C:\Users\LNV\Documents\5Code\FE-Blue-Code"
   ```
2. Tìm IP của máy: `ipconfig` (Windows) hoặc `ifconfig` (Mac/Linux)
3. Chạy một trong các lệnh sau:
   - PowerShell: `$env:HOST="0.0.0.0"; npm start`
   - CMD: `set HOST=0.0.0.0 && npm start`
   - Hoặc: `npm run dev:network`
4. Chia sẻ URL: `http://YOUR_IP:3000`

**Cho truy cập internet:**

1. Cài đặt và cấu hình Ngrok
2. Chạy: `npm start` và `ngrok http 3000`
3. Chia sẻ URL từ Ngrok
